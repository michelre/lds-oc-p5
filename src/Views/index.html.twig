{% extends "base.html.twig" %}

{% block title %}Accueil{% endblock %}

{% block content %}
    <!--PRODUITS-->

    <h2>NOS DERNIERS PRODUITS</h2>
    <div class="ligneproduits">
        <div class="ligneleft2"></div>
        <i class="fas fa-circle"></i>
        <div class="ligneright2"></div>
    </div>

<!--IMAGES-->

    <div class="contener">

        {% for product in products %}
        <a href="/P5/products/{{product.id}}">
           <figure><img src="{{product.image}}" alt="camera1" />
            <figcaption>
                <h4>{{product.title}}</h4></br>
                <p>{{product.description}}</p>
                <div class="camera"><i class="fas fa-camera-retro"></i>
                </div>
            </figcaption>
        </figure>
        </a>
        {% endfor %}

       

    </div>
{% endblock %}